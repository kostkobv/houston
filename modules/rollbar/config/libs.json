{
  "libs": [
    {
      "name": "ioredis",
      "options": {
        "port": "${redis:port}",
        "host": "${redis:host}"
      }
    },
    {
      "name": "restify"
    }
  ],
  "module": [
    "controllers",
    "routes",
    "services"
  ]
}